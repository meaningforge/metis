// Package extension defines preservation, validation, capability, and evidence
// contracts for semantic extensions. Critical unsupported semantics fail
// explicitly rather than being silently discarded.
package extension
