// Package resolver resolves semantic queries against manifest models and
// validates semantic reachability and compatibility before planning. It does
// not choose execution bindings or render physical SQL.
package resolver
