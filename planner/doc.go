// Package planner owns top-level SemanticPlan construction and optimization
// orchestration plus runtime observation. Domain contracts are exposed directly
// by evaluation, semanticplan, builder, optimizer, attribution, and conversion;
// planner does not alias or forward them.
package planner
