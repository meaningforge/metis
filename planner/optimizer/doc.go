// Package optimizer owns deterministic, semantics-preserving rewrites of an
// owned SemanticPlan. It does not resolve semantic names, construct SQLPlan,
// select a Renderer, render SQL, or execute queries.
package optimizer
